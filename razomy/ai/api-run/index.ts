// Imports
import * as batch from './batch';
import * as instant from './instant';

// Named exports
export {
  batch,
  instant
};

// Default export
const apiRun = {
  batch,
  instant,
};

export default apiRun;

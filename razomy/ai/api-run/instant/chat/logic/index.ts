// Imports
import { mapMessage } from './map_message';
import { mapMessageBack } from './map_message_back';
import { predict } from './predict';

// Named exports
export {
  mapMessage,
  mapMessageBack,
  predict
};

// Default export
const logic = {
  mapMessage,
  mapMessageBack,
  predict,
};

export default logic;

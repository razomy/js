// Imports
import type { ExtensionResult, FileCategory, FileExtensionType, FileFormat, MediaType } from './file_extension';

// Named exports
export type {
  ExtensionResult,
  FileCategory,
  FileExtensionType,
  FileFormat,
  MediaType
};


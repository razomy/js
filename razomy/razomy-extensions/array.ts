// AUTO-GENERATED FILE. DO NOT EDIT DIRECTLY.
import * as array from '@razomy/array';

// --- RUNTIME BINDINGS ---
if (!Array.prototype.addAllMut) {
  Object.defineProperty(Array.prototype, 'addAllMut', {
    value: function (...args: any[]) {
      return (array as any).addAllMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.addMut) {
  Object.defineProperty(Array.prototype, 'addMut', {
    value: function (...args: any[]) {
      return (array as any).addMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.and) {
  Object.defineProperty(Array.prototype, 'and', {
    value: function (...args: any[]) {
      return (array as any).and(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.chunk) {
  Object.defineProperty(Array.prototype, 'chunk', {
    value: function (...args: any[]) {
      return (array as any).chunk(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.countBy) {
  Object.defineProperty(Array.prototype, 'countBy', {
    value: function (...args: any[]) {
      return (array as any).countBy(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.difference) {
  Object.defineProperty(Array.prototype, 'difference', {
    value: function (...args: any[]) {
      return (array as any).difference(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.drop) {
  Object.defineProperty(Array.prototype, 'drop', {
    value: function (...args: any[]) {
      return (array as any).drop(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.every) {
  Object.defineProperty(Array.prototype, 'every', {
    value: function (...args: any[]) {
      return (array as any).every(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.filter) {
  Object.defineProperty(Array.prototype, 'filter', {
    value: function (...args: any[]) {
      return (array as any).filter(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.findIndex) {
  Object.defineProperty(Array.prototype, 'findIndex', {
    value: function (...args: any[]) {
      return (array as any).findIndex(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.find) {
  Object.defineProperty(Array.prototype, 'find', {
    value: function (...args: any[]) {
      return (array as any).find(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.firstEqualIndexes) {
  Object.defineProperty(Array.prototype, 'firstEqualIndexes', {
    value: function (...args: any[]) {
      return (array as any).firstEqualIndexes(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.flat) {
  Object.defineProperty(Array.prototype, 'flat', {
    value: function (...args: any[]) {
      return (array as any).flat(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.getFirst) {
  Object.defineProperty(Array.prototype, 'getFirst', {
    value: function (...args: any[]) {
      return (array as any).getFirst(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.getLast) {
  Object.defineProperty(Array.prototype, 'getLast', {
    value: function (...args: any[]) {
      return (array as any).getLast(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.groupBy) {
  Object.defineProperty(Array.prototype, 'groupBy', {
    value: function (...args: any[]) {
      return (array as any).groupBy(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.hasArray) {
  Object.defineProperty(Array.prototype, 'hasArray', {
    value: function (...args: any[]) {
      return (array as any).hasArray(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.includes) {
  Object.defineProperty(Array.prototype, 'includes', {
    value: function (...args: any[]) {
      return (array as any).includes(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.insertMut) {
  Object.defineProperty(Array.prototype, 'insertMut', {
    value: function (...args: any[]) {
      return (array as any).insertMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.intersection) {
  Object.defineProperty(Array.prototype, 'intersection', {
    value: function (...args: any[]) {
      return (array as any).intersection(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.isArrayEqual) {
  Object.defineProperty(Array.prototype, 'isArrayEqual', {
    value: function (...args: any[]) {
      return (array as any).isArrayEqual(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.isEmpty) {
  Object.defineProperty(Array.prototype, 'isEmpty', {
    value: function (...args: any[]) {
      return (array as any).isEmpty(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.mapToDictBy) {
  Object.defineProperty(Array.prototype, 'mapToDictBy', {
    value: function (...args: any[]) {
      return (array as any).mapToDictBy(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.mapToDict) {
  Object.defineProperty(Array.prototype, 'mapToDict', {
    value: function (...args: any[]) {
      return (array as any).mapToDict(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.map) {
  Object.defineProperty(Array.prototype, 'map', {
    value: function (...args: any[]) {
      return (array as any).map(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.reduce) {
  Object.defineProperty(Array.prototype, 'reduce', {
    value: function (...args: any[]) {
      return (array as any).reduce(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.removeAllMut) {
  Object.defineProperty(Array.prototype, 'removeAllMut', {
    value: function (...args: any[]) {
      return (array as any).removeAllMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.removeAtMut) {
  Object.defineProperty(Array.prototype, 'removeAtMut', {
    value: function (...args: any[]) {
      return (array as any).removeAtMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.removeFirstMut) {
  Object.defineProperty(Array.prototype, 'removeFirstMut', {
    value: function (...args: any[]) {
      return (array as any).removeFirstMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.removeLast) {
  Object.defineProperty(Array.prototype, 'removeLast', {
    value: function (...args: any[]) {
      return (array as any).removeLast(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.removeMut) {
  Object.defineProperty(Array.prototype, 'removeMut', {
    value: function (...args: any[]) {
      return (array as any).removeMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.reverseMut) {
  Object.defineProperty(Array.prototype, 'reverseMut', {
    value: function (...args: any[]) {
      return (array as any).reverseMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.reverse) {
  Object.defineProperty(Array.prototype, 'reverse', {
    value: function (...args: any[]) {
      return (array as any).reverse(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.setLastMut) {
  Object.defineProperty(Array.prototype, 'setLastMut', {
    value: function (...args: any[]) {
      return (array as any).setLastMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.set) {
  Object.defineProperty(Array.prototype, 'set', {
    value: function (...args: any[]) {
      return (array as any).set(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.some) {
  Object.defineProperty(Array.prototype, 'some', {
    value: function (...args: any[]) {
      return (array as any).some(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.sortByArrayMut) {
  Object.defineProperty(Array.prototype, 'sortByArrayMut', {
    value: function (...args: any[]) {
      return (array as any).sortByArrayMut(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.sortByFrequencyAndUnique) {
  Object.defineProperty(Array.prototype, 'sortByFrequencyAndUnique', {
    value: function (...args: any[]) {
      return (array as any).sortByFrequencyAndUnique(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.sortBy) {
  Object.defineProperty(Array.prototype, 'sortBy', {
    value: function (...args: any[]) {
      return (array as any).sortBy(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.take) {
  Object.defineProperty(Array.prototype, 'take', {
    value: function (...args: any[]) {
      return (array as any).take(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.toggle) {
  Object.defineProperty(Array.prototype, 'toggle', {
    value: function (...args: any[]) {
      return (array as any).toggle(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.tryFirstEqual) {
  Object.defineProperty(Array.prototype, 'tryFirstEqual', {
    value: function (...args: any[]) {
      return (array as any).tryFirstEqual(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.tryGetLastEqual) {
  Object.defineProperty(Array.prototype, 'tryGetLastEqual', {
    value: function (...args: any[]) {
      return (array as any).tryGetLastEqual(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.tryGetLast) {
  Object.defineProperty(Array.prototype, 'tryGetLast', {
    value: function (...args: any[]) {
      return (array as any).tryGetLast(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.union) {
  Object.defineProperty(Array.prototype, 'union', {
    value: function (...args: any[]) {
      return (array as any).union(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.uniqBy) {
  Object.defineProperty(Array.prototype, 'uniqBy', {
    value: function (...args: any[]) {
      return (array as any).uniqBy(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.uniq) {
  Object.defineProperty(Array.prototype, 'uniq', {
    value: function (...args: any[]) {
      return (array as any).uniq(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

if (!Array.prototype.zip) {
  Object.defineProperty(Array.prototype, 'zip', {
    value: function (...args: any[]) {
      return (array as any).zip(this as any, ...args);
    },
    writable: true,
    configurable: true
  });
}

// --- TYPESCRIPT DECLARATIONS ---
declare global {
  interface Array<T> {
    /** Based on: export function addAllMut<T>(array: T[], value: T[]): T[] {
     array.push(...value);
     return array;
     } */
    addAllMut<T>(this: T[], value: T[]): T[];

    /** Based on: export function addMut<T>(array: T[], value: T): T[] {
     array.push(value);
     return array;
     } */
    addMut<T>(this: T[], value: T): T[];

    /** Based on: export function and(...value: boolean[]) {
     return value.reduce((acc, cur) => acc && cur, true);
     } */
    and(this: boolean[],): boolean;

    /** Based on: export function chunk<T>(array: T[], size: number): T[][] {
     const result: T[][] = [];
     for (let i = 0; i < array.length; i += size) {
     result.push(array.slice(i, i + size));
     }
     return result;
     } */
    chunk<T>(this: T[], size: number): T[][];

    /** Based on: export function countBy<T>(array: ReadonlyArray<T>, predicate: (value: T) => any = (v) => v): Record<string, number> {
     const result: Record<string, number> = {};

     for (let i = 0; i < array.length; i++) {
     const key = String(predicate(array[i]));

     if (Object.prototype.hasOwnProperty.call(result, key)) {
     result[key]++;
     } else {
     result[key] = 1;
     }
     }

     return result;
     } */
    countBy<T>(this: ReadonlyArray<T>, predicate?: (value: T) => any): Record<string, number>;

    /** Based on: export function difference<T>(source: T[], other: T[]): T[] {
     const otherSet = new Set(other);
     return source.filter((item) => !otherSet.has(item));
     } */
    difference<T>(this: T[], other: T[]): T[];

    /** Based on: export function drop<T>(array: T[], count: number): T[] {
     return array.slice(count);
     } */
    drop<T>(this: T[], count: number): T[];

    /** Based on: export function every<T>(array: T[], predicate: (item: T, index: number, array: T[]) => boolean): boolean {
     for (let index = 0; index < array.length; index++) {
     if (!predicate(array[index], index, array)) {
     return false;
     }
     }

     return true;
     } */
    every<T>(this: T[], predicate: (item: T, index: number, array: T[]) => boolean): boolean;

    /** Based on: export function filter(
     array: any[],
     predicate: (value: any, index: number, array: any[]) => any
     ): any[] {
     return array.filter(predicate);
     } */
    filter(this: any[], predicate: (value: any, index: number, array: any[]) => any): any[];

    /** Based on: export function findIndex<T>(array: T[], predicate: (item: T, index: number, array: T[]) => boolean): number {
     const index = array.findIndex(predicate);

     if (index === -1) {
     throw new Error('Item not found.');
     }

     return index;
     } */
    findIndex<T>(this: T[], predicate: (item: T, index: number, array: T[]) => boolean): number;

    /** Based on: export function find<T>(array: T[], predicate: (item: T, index: number, array: T[]) => boolean): T {
     return array[array_.findIndex(array, predicate)];
     } */
    find<T>(this: T[], predicate: (item: T, index: number, array: T[]) => boolean): T;

    /** Based on: export function firstEqualIndexes<T>(a: T[], b: T[], startA = 0, startB = 0) {
     let aIndex = a.length;
     let bIndex = b.length;
     let potentialIndex = aIndex + bIndex;
     let aMax = Math.min(a.length, potentialIndex);
     let bMax = Math.min(b.length, potentialIndex);

     for (let aI = startA; aI < aMax; aI++) {
     for (let bI = startB; bI < bMax; bI++) {
     const potential = aI + bI;
     if (potential > potentialIndex) {
     break;
     }
     if (a[aI] !== b[bI]) {
     continue;
     }
     if (potential < potentialIndex) {
     aIndex = aI;
     bIndex = bI;
     potentialIndex = potential;
     aMax = Math.min(a.length, potentialIndex);
     bMax = Math.min(b.length, potentialIndex);
     }
     }
     }
     return [aIndex, bIndex];
     } */
    firstEqualIndexes<T>(this: T[], b: T[], startA?: number, startB?: number): number[];

    /** Based on: export function flat<T>(array: readonly (T | ReadonlyArray<T>)[]): T[] {
     return array.flat(1) as T[];
     } */
    flat<T>(this: readonly (T | ReadonlyArray<T>)[],): T[];

    /** Based on: export function getFirst<T>(array: T[]): T {
     if (array.length === 0) {
     throw new Error('Array is empty');
     }

     return array[0];
     } */
    getFirst<T>(this: T[],): T;

    /** Based on: export function getLast<T>(array: T[], offset: number = 0): T {
     const item = array.at(-1 - offset);

     if (item === undefined) {
     throw new Error(`Element at offset ${offset} does not exist.`);
     }

     return item;
     } */
    getLast<T>(this: T[], offset?: number): T;

    /** Based on: export function groupBy<T, K extends PropertyKey>(array: T[], iteratee: (item: T) => K): Record<K, T[]> {
     return array.reduce((result, item) => {
     const key = iteratee(item);

     if (!result[key]) {
     result[key] = [];
     }

     result[key].push(item);

     return result;
     }, {} as Record<K, T[]>);
     } */
    groupBy<T, K extends PropertyKey>(this: T[], iteratee: (item: T) => K): Record<K, T[]>;

    /** Based on: export function hasArray<T>(source: T[], array: T[]): boolean {
     let offset = 0;

     for (const item of array) {
     const foundIndex = source.indexOf(item, offset);

     if (foundIndex === -1) {
     return false;
     }

     offset = foundIndex + 1;
     }

     return true;
     } */
    hasArray<T>(this: T[], array: T[]): boolean;

    /** Based on: export function includes<T>(array: T[], value: T, fromIndex?: number): boolean {
     return array.includes(value, fromIndex);
     } */
    includes<T>(this: T[], value: T, fromIndex?: number): boolean;

    /** Based on: export function insertMut<T>(array: T[], index: number, item: T): T[] {
     array.splice(index, 0, item);
     return array;
     } */
    insertMut<T>(this: T[], index: number, item: T): T[];

    /** Based on: export function intersection<T>(source: T[], target: T[]): T[] {
     const targetSet = new Set(target);
     return Array.from(new Set(source)).filter((item) => targetSet.has(item));
     } */
    intersection<T>(this: T[], target: T[]): T[];

    /** Based on: export function isArrayEqual<T>(arr1: T[], arr2: T[]) {
     return arr1.length === arr2.length && arr1.every((val, index) => val === arr2[index]);
     } */
    isArrayEqual<T>(this: T[], arr2: T[]): boolean;

    /** Based on: export function isEmpty<T>(array: ReadonlyArray<T>): boolean {
     return array.length === 0;
     } */
    isEmpty<T>(this: ReadonlyArray<T>,): boolean;

    /** Based on: export function mapToDictBy<T, K extends keyof T>(
     arr: T[],
     get: (i: T) => string,
     ): Record<string | number | symbol, T> {
     return arr.reduce((acc, item) => {
     // Correctly index item[key] and assign to accumulator
     acc[get(item)] = item;
     return acc;
     }, {} as Record<string | number | symbol, T>);
     } */
    mapToDictBy<T, K extends keyof T>(this: T[], get: (i: T) => string): Record<string | number | symbol, T>;

    /** Based on: export function mapToDict<T, K extends keyof T>(arr: T[], key: K): Record<string | number | symbol, T> {
     return arr.reduce((acc, item) => {
     // Correctly index item[key] and assign to accumulator
     acc[item[key] as any] = item;
     return acc;
     }, {} as Record<string | number | symbol, T>);
     } */
    mapToDict<T, K extends keyof T>(this: T[], key: K): Record<string | number | symbol, T>;

    /** Based on: export function map<T, U>(array: T[], iteratee: (element: T, index: number, array: T[]) => U): U[] {
     const length = array.length;
     const result = new Array<U>(length);

     for (let index = 0; index < length; index++) {
     result[index] = iteratee(array[index], index, array);
     }

     return result;
     } */
    map<T, U>(this: T[], iteratee: (element: T, index: number, array: T[]) => U): U[];

    /** Based on: export function reduce<T, A>(
     array: T[],
     reducer: (accumulator: A, value: T, index: number, array: T[]) => A,
     initialValue: A,
     ): A {
     let accumulator = initialValue;
     for (let i = 0; i < array.length; i++) {
     accumulator = reducer(accumulator, array[i], i, array);
     }
     return accumulator;
     } */
    reduce<T, A>(this: T[], reducer: (accumulator: A, value: T, index: number, array: T[]) => A, initialValue: A): A;

    /** Based on: export function removeAllMut<T>(array: T[], values: T[]): void {
     for (const value of values) {
     array_.removeFirstMut(array, value);
     }
     } */
    removeAllMut<T>(this: T[], values: T[]): void;

    /** Based on: export function removeAtMut<T>(arr: T[], index: number): T | undefined {
     return arr.splice(index, 1)[0];
     } */
    removeAtMut<T>(this: T[], index: number): T | undefined;

    /** Based on: export function removeFirstMut<T>(array: T[], value: T): void {
     const index: number = array.indexOf(value);

     if (index !== -1) {
     array.splice(index, 1);
     }
     } */
    removeFirstMut<T>(this: T[], value: T): void;

    /** Based on: export function removeLast<T>(arr: ReadonlyArray<T>, deltaIndex: number = 0): T[] {
     const end: number = arr.length - 1 + deltaIndex;

     if (end <= 0) {
     return [];
     }

     return arr.slice(0, end);
     } */
    removeLast<T>(this: ReadonlyArray<T>, deltaIndex?: number): T[];

    /** Based on: export function removeMut<T>(arr: T[], item: T): T | undefined {
     const arrIndex = arr.findIndex(i => i === item);
     return array.removeAtMut(arr, arrIndex);
     } */
    removeMut<T>(this: T[], item: T): T | undefined;

    /** Based on: export function reverseMut<T>(array: T[]): T[] {
     return array.reverse();
     } */
    reverseMut<T>(this: T[],): T[];

    /** Based on: export function reverse<T>(array: T[]): T[] {
     return [...array].reverse();
     } */
    reverse<T>(this: T[],): T[];

    /** Based on: export function setLastMut<T>(array: T[], value: T, offset: number = 0): T[] {
     array[array.length - 1 + offset] = value;
     return array;
     } */
    setLastMut<T>(this: T[], value: T, offset?: number): T[];

    /** Based on: export function set<T>(array: Array<T>, index: number, value: T): T[] {
     return array.with(index, value);
     } */
    set<T>(this: Array<T>, index: number, value: T): T[];

    /** Based on: export function some<T>(array: T[], predicate: (item: T, index: number, array: T[]) => boolean): boolean {
     return array.some(predicate);
     } */
    some<T>(this: T[], predicate: (item: T, index: number, array: T[]) => boolean): boolean;

    /** Based on: export function sortByArrayMut<T>(oldOrder: T[], newOrder: T[]): T[] {
     const orderMap = new Map<T, number>();

     for (let i = 0; i < newOrder.length; i++) {
     orderMap.set(newOrder[i], i);
     }

     oldOrder.sort((a, b) => (orderMap.get(a) ?? Infinity) - (orderMap.get(b) ?? Infinity));

     return oldOrder;
     } */
    sortByArrayMut<T>(this: T[], newOrder: T[]): T[];

    /** Based on: export function sortByFrequencyAndUnique<T>(array: T[]): T[] {
     const frequencies = array_.countBy(array);
     const distinctItems = array_.uniq(array);

     return array_.sortBy(distinctItems, (item: T) => {
     return -frequencies[item as any];
     });
     } */
    sortByFrequencyAndUnique<T>(this: T[],): T[];

    /** Based on: export function sortBy<T>(array: T[], iteratee: (item: T) => string | number = (i) => i as any): T[] {
     const length = array.length;
     const mapped = new Array(length);

     for (let i = 0; i < length; i++) {
     const value = array[i];
     mapped[i] = {
     index: i,
     value,
     criteria: iteratee(value),
     };
     }

     mapped.sort((a, b) => {
     const criteriaA = a.criteria;
     const criteriaB = b.criteria;

     if (criteriaA > criteriaB) {
     return 1;
     }

     if (criteriaA < criteriaB) {
     return -1;
     }

     return a.index - b.index;
     });

     const result = new Array(length);
     for (let i = 0; i < length; i++) {
     result[i] = mapped[i].value;
     }

     return result;
     } */
    sortBy<T>(this: T[], iteratee?: (item: T) => string | number): T[];

    /** Based on: export function take<T>(array: T[], n: number): T[] {
     return array.slice(0, n);
     } */
    take<T>(this: T[], n: number): T[];

    /** Based on: export function toggle<T>(array: T[], item: T): T[] {
     const index = array.indexOf(item);

     if (index === -1) {
     return [...array, item];
     }

     return [...array.slice(0, index), ...array.slice(index + 1)];
     } */
    toggle<T>(this: T[], item: T): T[];

    /** Based on: export function tryFirstEqual<T>(array1: T[], array2: T[]): T | null {
     for (const item2 of array2) {
     for (const item1 of array1) {
     if (item2 === item1) {
     return item2;
     }
     }
     }

     return null;
     } */
    tryFirstEqual<T>(this: T[], array2: T[]): T | null;

    /** Based on: export function tryGetLastEqual<T, U>(
     arrayA: T[],
     arrayB: U[],
     predicate: (itemA: T, itemB: U) => boolean,
     ): [T, U] | null {
     for (let i = arrayA.length - 1; i >= 0; i--) {
     const itemA = arrayA[i];
     for (let j = arrayB.length - 1; j >= 0; j--) {
     const itemB = arrayB[j];
     if (predicate(itemA, itemB)) {
     return [itemA, itemB];
     }
     }
     }

     return null;
     } */
    tryGetLastEqual<T, U>(this: T[], arrayB: U[], predicate: (itemA: T, itemB: U) => boolean): [T, U] | null;

    /** Based on: export function tryGetLast<T>(arr: T[], deltaIndex: number = 0): T | null {
     return arr.at(-1 + deltaIndex) ?? null;
     } */
    tryGetLast<T>(this: T[], deltaIndex?: number): T | null;

    /** Based on: export function union<T>(...arrays: T[][]): T[] {
     return Array.from(new Set(arrays.flat()));
     } */
    union<T>(this: T[][],): T[];

    /** Based on: export function uniqBy<T, U>(
     array: ReadonlyArray<T>,
     iteratee: (value: T) => U
     ): T[] {
     const seen = new Set<U>();
     const result: T[] = [];

     for (const item of array) {
     const key = iteratee(item);
     if (!seen.has(key)) {
     seen.add(key);
     result.push(item);
     }
     }

     return result;
     } */
    uniqBy<T, U>(this: ReadonlyArray<T>, iteratee: (value: T) => U): T[];

    /** Based on: export function uniq<T>(array: ReadonlyArray<T>): T[] {
     return Array.from(new Set(array));
     } */
    uniq<T>(this: ReadonlyArray<T>,): T[];

    /** Based on: export function zip<T>(...arrays: T[][]): T[][] {
     const lengths = arrays.map((array) => array.length);
     const limit = lengths.length > 0 ? Math.min(...lengths) : 0;
     const zipped: T[][] = [];

     for (let index = 0; index < limit; index++) {
     zipped.push(arrays.map((array) => array[index]));
     }

     return zipped;
     } */
    zip<T>(this: T[][],): T[][];
  }
}
export {};

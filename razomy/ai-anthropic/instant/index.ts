// Imports
import { ask } from './ask';
import { askTool } from './ask_tool';
import { specToTool } from './spec_to_tool';

// Named exports
export {
  ask,
  askTool,
  specToTool
};

// Default export
const instant = {
  ask,
  askTool,
  specToTool,
};

export default instant;

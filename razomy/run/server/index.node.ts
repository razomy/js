// Imports
import { call } from './call';
import { start } from './start.node';

// Named exports
export {
  call,
  start
};

// Default export
const server = {
  call,
  start,
};

export default server;

// Imports
import { spawnProcess } from './spawn_process.node';
import { start } from './start.node';

// Named exports
export {
  spawnProcess,
  start
};

// Default export
const cli = {
  spawnProcess,
  start,
};

export default cli;

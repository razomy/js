// Imports
import { isShape } from './is_shape';
import { parse } from './parse';

// Named exports
export {
  isShape,
  parse
};

// Default export
const shapes = {
  isShape,
  parse,
};

export default shapes;

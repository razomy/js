// Imports
import { askTool } from './ask_tool';
import { specToTool } from './spec_to_tool';

// Named exports
export {
  askTool,
  specToTool
};

// Default export
const instant = {
  askTool,
  specToTool,
};

export default instant;

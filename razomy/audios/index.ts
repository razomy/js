// Imports
import { ALL_AUDIO_TARGETS, AUDIOS, AUDIO_CONVERSIONS, AUDIO_EXTENSIONS } from './types';

// Named exports
export {
  ALL_AUDIO_TARGETS,
  AUDIOS,
  AUDIO_CONVERSIONS,
  AUDIO_EXTENSIONS
};

// Default export
const audios = {
  ALL_AUDIO_TARGETS,
  AUDIOS,
  AUDIO_CONVERSIONS,
  AUDIO_EXTENSIONS,
};

export default audios;

// Imports
import * as length from './length';
import * as rectangle from './rectangle';
import * as x from './x';
import * as y from './y';

// Named exports
export {
  length,
  rectangle,
  x,
  y
};

// Default export
const coordinates = {
  length,
  rectangle,
  x,
  y,
};

export default coordinates;

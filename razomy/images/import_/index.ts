// Imports
import { createBlank } from './create_blank';
import { getFile } from './get_file';
import { setFile } from './set_file';

// Named exports
export {
  createBlank,
  getFile,
  setFile
};

// Default export
const import_ = {
  createBlank,
  getFile,
  setFile,
};

export default import_;

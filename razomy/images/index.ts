// Imports
import * as advanced from './advanced';
import * as color from './color';
import * as compositing from './compositing';
import * as export_ from './export_';
import * as filter from './filter';
import * as geometry from './geometry';
import * as import_ from './import_';
import type { Image } from './type';

// Named exports
export {
  advanced,
  color,
  compositing,
  export_,
  filter,
  geometry,
  import_
};
export type {
  Image
};

// Default export
const images = {
  advanced,
  color,
  compositing,
  export_,
  filter,
  geometry,
  import_,
};


export default images;

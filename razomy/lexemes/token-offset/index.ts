// Imports
import { tryAll } from './try_all';
import { tryTokenValue } from './try_token_value';

// Named exports
export {
  tryAll,
  tryTokenValue
};

// Default export
const tokenOffset = {
  tryAll,
  tryTokenValue,
};

export default tokenOffset;

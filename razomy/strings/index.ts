// Imports
import * as path from './path';
import { separateStrings } from './separate_strings';

// Named exports
export {
  path,
  separateStrings
};

// Default export
const strings = {
  path,
  separateStrings,
};

export default strings;

// Imports
import { differences_ } from './differences_';
import { differencesString } from './differences_string';
import { getDifference } from './get_difference';

// Named exports
export {
  differences_,
  differencesString,
  getDifference
};

// Default export
const differences = {
  differences_,
  differencesString,
  getDifference,
};

export default differences;

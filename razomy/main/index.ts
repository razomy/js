// Imports
import { ifMain } from './if_main';
import { isMain } from './is_main';

// Named exports
export {
  ifMain,
  isMain
};

// Default export
const main = {
  ifMain,
  isMain,
};

export default main;

// Imports
import { REGEX, acceptLanguageParse } from './accept_language_parse';
import { acceptLanguagePick } from './accept_language_pick';

// Named exports
export {
  REGEX,
  acceptLanguageParse,
  acceptLanguagePick
};

// Default export
const httpLocalisation = {
  REGEX,
  acceptLanguageParse,
  acceptLanguagePick,
};

export default httpLocalisation;

// Imports
import { first } from './first';
import type { HasParent } from './parent';

// Named exports
export {
  first
};
export type {
  HasParent
};

// Default export
const parent = {
  first,
};


export default parent;

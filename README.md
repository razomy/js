### Contribution 
#### Checklist
- one element per file
- one element per system
- name is atomic, extendable with prefixes and suffixes
- always throw
- no input validation
- try_ prefix for optional execution
- no optional execution
- with_ interface prefix
- variable, property, function snake_case
- class, type, interface, enum PascalCase
- same name everywhere, instant, filename, property
- priority to export default
- import is 'razomy....'
- no i prefix